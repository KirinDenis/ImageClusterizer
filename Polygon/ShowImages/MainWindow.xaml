<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ShowImages.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShowImages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ShowImages">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>


        <Viewbox  VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <GridView ItemsSource="{x:Bind ViewModel.IntCol, Mode=OneWay}" Height="1000" Width="1000">
            <GridView.ItemTemplate >
                <DataTemplate x:DataType="local:ImageData" >
                    <Image Source="{x:Bind FileName}" Width="100" Height="100" />
                </DataTemplate>
            </GridView.ItemTemplate>
            
            
        </GridView>
            </Viewbox>

        </Grid>
</Window>
